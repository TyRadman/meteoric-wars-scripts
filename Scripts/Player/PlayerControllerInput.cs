using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SpaceWar
{
    public class PlayerControllerInput : MonoBehaviour
    {
        public void Activate(bool _enable)
        {
            //if (Active) SetUpAbilities();
        }
    }
}